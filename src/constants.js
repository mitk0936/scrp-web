const STATUS = {
  RUNNING: 'running',
  IDLE: 'idle',
  FINISHED: 'finished'
};

const EVENT_TYPES = {
  WARNING: 'warning',
  SUCCESS: 'success',
  FAILURE: 'failure'
};

module.exports = { STATUS, EVENT_TYPES };